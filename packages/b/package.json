{
  "name": "src2",
  "version": "1.0.0",
  "dependencies": {
  },
  "devDependencies": {},
  "scripts": {
    "build": "rm -rf ../../built && npm run tsc",
    "tsc": "../../node_modules/.bin/tsc -b"
  }
}
